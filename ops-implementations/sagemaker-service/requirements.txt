connexion[swagger-ui]==2.14.1

# --- Dependences of connexion[swagger-ui] that need to be freezed --- start
openapi-schema-validator==0.3.4
# --- Dependences of connexion[swagger-ui] --- end

# --- Dependences of sagemaker that need to be freezed --- start
protobuf==3.20.3
# --- Dependences of sagemaker --- end

boto3==1.24.84
sagemaker==2.110.0
